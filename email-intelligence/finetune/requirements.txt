# Install in D:\miniconda3\envs\mlenv (Python 3.11)
# Run via train.bat — NOT the system Python

# Core ML stack
torch>=2.7.0            # install separately: pip install torch --index-url https://download.pytorch.org/whl/cu118
transformers==4.46.3
trl==0.11.4
peft>=0.12.0
accelerate==0.34.2
bitsandbytes>=0.43.0

# Data
datasets>=2.14.0
pyarrow

# Evaluation
scikit-learn>=1.3.0

# Utils
python-dotenv
rich